// TODO Run creat_tables.sql

// TODO Run input_data.sql

// TODO Rename route.name and mission.name

// const initData = {
//     async addNameForRouteAndMission() {
        
//     } 
// }
// function async 
// UPDATE "route" SET "name"=concat_ws(" to ", least(join)

// select t.*,
//        concat_ws(':', least(origin, destination) as city1,
//                  greatest(origin, destination) 
//                 ) as route_name
// from t;

// SELECT figurine.*, AVG(review.note) AS note_average, count(review.note) as note_count 
// FROM figurine JOIN review ON review.figurine_id = figurine.id WHERE figurine.id=$1 GROUP BY figurine.id;

// COMMIT;